import 'package:flutter/material.dart';

class AppColors {
  // static const Color primaryColor = Color(0xFFB0BEC5);  // Background color for buttons, title bar, table header, and icons
  static const Color primaryColor = Color(0xFFA6B2B8);
  static const Color foregroundColor = Colors.white;     // White as the foreground color
}
